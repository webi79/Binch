diff --git a/node_modules/react-native-bottom-tabs/android/src/main/java/com/rcttabview/RCTTabView.kt b/node_modules/react-native-bottom-tabs/android/src/main/java/com/rcttabview/RCTTabView.kt
index 1d0c404..1e6c7df 100644
--- a/node_modules/react-native-bottom-tabs/android/src/main/java/com/rcttabview/RCTTabView.kt
+++ b/node_modules/react-native-bottom-tabs/android/src/main/java/com/rcttabview/RCTTabView.kt
@@ -38,6 +38,22 @@ import com.google.android.material.navigation.NavigationBarView.LABEL_VISIBILITY
 import com.google.android.material.transition.platform.MaterialFadeThrough
 
 class ExtendedBottomNavigationView(context: Context) : BottomNavigationView(context) {
+  init {
+    // Binch-Patch: Material's BottomNavigationView wendet in seinem internen
+    // OnApplyWindowInsetsListener die legacy systemWindowInsets an — die
+    // schließen den IME ein. Mit Edge-to-Edge hebt sich die Bar dadurch
+    // über die geöffnete Tastatur. Wir ersetzen den Listener (nur einer pro
+    // View möglich) durch einen, der ausschließlich die System-Bars als
+    // Bottom-Padding anwendet und die Tastatur ignoriert → Bar bleibt
+    // statisch am Screen-Bottom.
+    val initialBottom = paddingBottom
+    androidx.core.view.ViewCompat.setOnApplyWindowInsetsListener(this) { v, insets ->
+      val bars = insets.getInsets(androidx.core.view.WindowInsetsCompat.Type.systemBars())
+      v.setPadding(v.paddingLeft, v.paddingTop, v.paddingRight, initialBottom + bars.bottom)
+      insets
+    }
+  }
+
   override fun getMaxItemCount(): Int {
     return 100
   }
@@ -205,6 +221,9 @@ class ReactBottomNavigationView(context: Context) : LinearLayout(context) {
   private fun toggleViewVisibility(view: View, isVisible: Boolean) {
     check(view is ViewGroup) { "Native component tree is corrupted." }
 
+    // (Binch: bewusst GONE — INVISIBLE-Views nehmen an jedem Measure/Layout-
+    // Pass des Containers teil, d.h. ALLE besuchten Tab-Trees inkl. Map
+    // wuerden bei jedem Layout-Anlass mitvermessen. GONE wird uebersprungen.)
     view.visibility = if (isVisible) VISIBLE else GONE
     view.isEnabled = isVisible
   }
diff --git a/node_modules/react-native-bottom-tabs/lib/module/DelayedFreeze.js b/node_modules/react-native-bottom-tabs/lib/module/DelayedFreeze.js
index 29668ba..29af5d8 100644
--- a/node_modules/react-native-bottom-tabs/lib/module/DelayedFreeze.js
+++ b/node_modules/react-native-bottom-tabs/lib/module/DelayedFreeze.js
@@ -4,7 +4,7 @@ import React from 'react';
 import { Freeze } from 'react-freeze';
 import { jsx as _jsx } from "react/jsx-runtime";
 // Animation delay for freeze effect.
-const ANIMATION_DELAY = 200;
+const ANIMATION_DELAY = 0; // Binch-Patch, siehe src/DelayedFreeze.tsx
 
 // This component delays the freeze effect by animation delay.
 // So that the screen is not frozen immediately causing background flash.
diff --git a/node_modules/react-native-bottom-tabs/src/DelayedFreeze.tsx b/node_modules/react-native-bottom-tabs/src/DelayedFreeze.tsx
index 3e49daf..be42355 100644
--- a/node_modules/react-native-bottom-tabs/src/DelayedFreeze.tsx
+++ b/node_modules/react-native-bottom-tabs/src/DelayedFreeze.tsx
@@ -7,7 +7,13 @@ interface FreezeWrapperProps {
 }
 
 // Animation delay for freeze effect.
-const ANIMATION_DELAY = 200;
+// Binch-Patch: 0 statt 200ms. Der 200ms-Delay existiert nur gegen den
+// Background-Flash WÄHREND einer Page-Wechsel-Animation — wir fahren
+// disablePageAnimations, der Wechsel ist instant. Mit 200ms landete der
+// Freeze-Commit des verlassenen Tabs (Suspense-Hide + Effect-Cleanups)
+// exakt im Scroll-Fenster nach dem Tab-Wechsel → sichtbare Ruckler auf
+// dem Ziel-Tab. Mit 0 passiert er im (eh busy) Wechsel-Moment.
+const ANIMATION_DELAY = 0;
 
 // This component delays the freeze effect by animation delay.
 // So that the screen is not frozen immediately causing background flash.
